import { EPSILON, DOLLAR, FIRST_TERMINAL, EPSILON_STR } from '../../scannerparser/Constants';
import { List } from '../../DataStructures';
import { FiniteAutomata } from '../FiniteAutomata';

export class Grammar{
/*
    protected symbols: string[] = [];
    protected startSymbol: number;
    public tokenNameList: List<string>;
    public FIRST_NON_TERMINAL: number = 0;
    public SEMANTIC_ACTION_COUNT:number = 0;
*/  
    // public int FIRST_SEMANTIC_ACTION() { return symbols.length; }
    // public int LAST_SEMANTIC_ACTION() { return FIRST_SEMANTIC_ACTION()+SEMANTIC_ACTION_COUNT; 

}